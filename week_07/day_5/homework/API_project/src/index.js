import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Render the App component to the element with the ID "root"
ReactDOM.render(<App />, document.getElementById("root"));